node_modules/
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local
GoogleService-Info.plist
google-services.json

# One-time cleanup scripts (sensitive — contain Firebase config, not for CI).
# Ignore scripts/ contents (scripts/* so specific files can be re-included).
scripts/*
!scripts/patch-datetimepicker.js
!scripts/backfill-clear-local-photourls.js
# Build-time CI checks (no secrets) — tracked despite the scripts/ ignore above.
!scripts/validate-signin-slides.js
!scripts/test-signin-contract.js

# Firebase Cloud Functions build output
functions/lib/
functions/node_modules/
ios/
