{
  "name": "swapdog-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore & Storage security-rules emulator tests for SwapDog.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project swapdog-rules-test --config ../../firebase.json \"jest --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "firebase-tools": "^13.29.1",
    "jest": "^29.7.0"
  }
}

